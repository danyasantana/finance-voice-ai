package com.finance.voice.infrastructure.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AiConfig {

    // ChatClient is configured in ChatService with default tools
    // This configuration class can be used for other AI-related beans
}
